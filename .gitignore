node_modules
dist
.expo
.turbo
*.log
.DS_Store
.env
.env.local
# A worktree's own slot — Metro port, simulator, database mode. Tracking it
# would hand one worktree's resources to the next checkout that reads it.
.env.worktree
.idea/
.worktree/
tmp/
.vercel
