# Planazo redesign — handoff notes

_Last updated 2026-07-28, after PR #5 (plan detail) merged._

**Architecture (settled, do not relitigate):** React Native/Expo (SDK 54), Supabase-only backend (RLS + SECURITY DEFINER RPCs; Edge Functions later for push/AI; no Express — deleted in Phase 0 after explicit discussion). User-facing word is **"Groups"** (never "Circles"); profile is the **12b/14a menu-style sheet** (read-only, Edit button, Send-feedback row). The user is sensitive to unflagged structural changes AND to deviations from the design doc — if implementing less/differently than the design shows, say so explicitly first.

**Design source of truth:** claude.ai/design project `41010790-a39d-4649-a417-78bba9ae97a5`, file `Planazo Screens Final.dc.html` (fetch via DesignSync get_file; ~247KB, decode `.content` with jq). Screen anchors: 2a feed, 5a create sheet, 6a–6e groups, 8a/8b/9a plan detail, 10–12 profile, 14a–c feedback, 16 empty states, 18a/b invites.

**Merged (main, PRs #2–#5):** Phase 0 foundation (shared plan-logic + 34 tests, generated DB types via `pnpm db:gen:types`, jest-expo+RNTL, CI, RPC migrations); Phase 1 design system (`theme/tokens.ts`, 12 components in `components/ui/`, dev gallery at `planazo://design-gallery`, fonts Bricolage Grotesque + Instrument Sans); Phase 2 shell + feed (custom TabBar Plans/+/Groups, feed with inline answering incl. 2a inline date-picking, `app/dev-login.tsx`); Phase 3 plan detail (8a/8b/9a: status card + SlotBar headline, inline voting with Leading tag, host Lock in/Reopen/Cancel via RPCs `lock_plan`/`reopen_plan`/`cancel_plan`; lock converts availability→yes-RSVPs). 35 mobile tests + 34 shared tests, all green.

**Next slices in order:**
1. **Create sheet (5a)** — rebuild `app/(app)/plan/create.tsx`: title, group picker (pre-filled when opened from a group), calendar where 1 tapped day = fixed (time field appears) and 2+ = flexible, floor-and-ceiling card for min/max. Design markup at anchor 5a (~line 2253 of the design file).
2. **Groups suite (6a–6e, 18a/b)** — group list, invites collapsed row + sheet, group detail, invite screen, manage (roles/prefs/leave). Needs migrations: `color` on groups, an invites table.
3. **Profile + feedback (12b/12c, 14a–c)** — menu sheet off feed avatar, edit name/photo, feedback flow with auto-screenshot (react-native-view-shot). Needs: `handle` on profiles, feedback table + storage.
4. **Endgame** — push notifications (expo-notifications + Edge Function, `push_token` column and `notifications` table already exist), cancelled/expired states, integration test suite against local Supabase in CI.

**Workflow a fresh session must know:** Old detail/groups screens' patterns are replaced — style ONLY from `theme/tokens.ts` + `components/ui`. Slice = branch off main → screens+tests → typecheck (`npx tsc --noEmit` in apps/mobile) + `npx jest --forceExit` → simulator verify → PR (merge only when user says). Simulator: iPhone 16 Pro (UDID CA8D5129-C019-44E6-8909-070CDD2924CE), Metro on **8083** (8081 belongs to user's thrive project), start `npx expo start --port 8083` in apps/mobile, connect app via `xcrun simctl openurl <udid> "com.planazo.app://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8083"`, auth via `planazo://dev-login?email=demo.planazo%40example.com&password=Planazo123%21`, screens via `planazo://plan/<id>` etc. Dev-menu onboarding blocker: `xcrun simctl spawn <udid> defaults write com.planazo.app isOnboardingFinished -bool true`. No tap capability — design screens to be reachable/verifiable by deep link; gallery supports `?y=<px>`/`?tail=1` for scroll-less screenshots. RNTL v14: `render`/`fireEvent` are async, use `screen.*`; supabase mock chain pattern lives in `app/(app)/(tabs)/__tests__/index.test.tsx`. DB pushes: see the repo root `.env` (SUPABASE_DB_URL; push with `supabase db push --db-url "$SUPABASE_DB_URL"` — the CLI login on this machine lacks access to the project). Native dep added → rebuild `npx expo run:ios --device "iPhone 16 Pro" --no-bundler`.
