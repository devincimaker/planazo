{
  "name": "@planazo/integration-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "//test": "Deliberately NOT named `test`: turbo would then sweep this suite into `turbo test`, so the documented gate (`pnpm turbo typecheck lint test`, then `pnpm test:integration`) ran all 137 of these against a real database twice. `turbo test` now means unit tests, which need no database and run anywhere.",
    "test:integration": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@planazo/shared": "workspace:*",
    "@supabase/supabase-js": "^2.89.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.3",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
